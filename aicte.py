"""
AICTE adapter.

No unauthenticated public jobs API contract is assumed here. Keep this source
as a disabled placeholder until the exact current API/feed is verified.
"""
def fetch(*args, **kwargs):
    print("AICTE: disabled until a current public jobs API/feed is configured.")
    return []
